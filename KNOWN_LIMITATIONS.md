# Known limitations

- The interface does not convert, upload, persist, or inspect real drawing files.
- All displayed analysis values are deterministic synthetic mock values.
- Production architecture, APIs, infrastructure, datasets, and commercial implementation are deliberately excluded.
